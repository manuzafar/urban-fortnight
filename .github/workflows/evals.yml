name: Agent Evals

on:
  push:
    branches: [main]
    paths:
      - 'backend/agents/**'
      - 'backend/models/**'
      - 'backend/evals/**'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Evals would run here"
